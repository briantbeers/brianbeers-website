/**
 * Legacy path (pre–Content Layer).
 * Canonical collection schema: src/content.config.ts
 * Astro loads src/content.config.ts; this file is intentionally empty of
 * collections so it does not duplicate or conflict.
 */
