# CMS — Keystatic

See README.md section **CMS (Keystatic)** for the full how-to.

Quick start: `npm run dev` then open http://127.0.0.1:4321/keystatic

Schema: src/content.config.ts + keystatic.config.ts (SCHEMA.md).
Agents edit src/content/models/*.md on disk; slug = filename.
